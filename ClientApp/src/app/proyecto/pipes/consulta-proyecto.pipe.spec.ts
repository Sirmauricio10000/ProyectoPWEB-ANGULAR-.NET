import { ConsultaProyectoPipe } from './consulta-proyecto.pipe';

describe('ConsultaProyectoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultaProyectoPipe();
    expect(pipe).toBeTruthy();
  });
});
